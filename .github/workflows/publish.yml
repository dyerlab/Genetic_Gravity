name: Build and deploy manuscript site

on:
  push:
    branches: [main]
    paths:
      - "*.md"
      - "*.meta.yml"
      - "media/**"
      - ".github/workflows/publish.yml"
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  site:
    uses: dyerlab/manuscript-theme/.github/workflows/render.yml@v1
    with:
      markdown_files: "index.md supplemental.md"
